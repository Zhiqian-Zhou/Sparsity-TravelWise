# Pinned dependencies for the European Railway Stop-Level Disruption pipeline.
# These are the versions the codebase was validated against. Newer majors
# (numpy 2.x, xgboost 3.x, lightgbm 4.6+) are known to break smoke tests or
# the early-stopping API — pin to the ranges below until those are fixed.
#
# Recommended workflow on a fresh machine:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   # Optional GNN stack (Phase 3 graphsage model):
#   pip install -r requirements-gnn.txt    # if you have one, else next 2 lines
#   pip install torch==2.2.2 torch_geometric==2.5.3

# ── Core data + ML ────────────────────────────────────────────────────────────
pandas==2.0.3
numpy==1.26.4                 # 2.x breaks the BiLSTM/torch interop smoke test
scikit-learn==1.3.2
lightgbm==4.3.0               # 4.6+ changed the early-stopping callback API
xgboost==2.0.3                # 3.x is untested here
shap==0.45.1
pyarrow==15.0.2
networkx==3.2.1

# ── Plotting ──────────────────────────────────────────────────────────────────
matplotlib==3.8.4
seaborn==0.13.2

# ── Notebooks (Phase 1) ───────────────────────────────────────────────────────
jupyter==1.0.0
papermill==2.5.0
nbconvert==7.16.4

# ── Optional: Netherlands weather enrichment (Phase 1) ────────────────────────
# pip install openmeteo-requests==1.2.0 requests-cache==1.2.0 retry-requests==2.0.0

# ── Optional: GNN model (Phase 3 graphsage) ───────────────────────────────────
# pip install torch==2.2.2 torch_geometric==2.5.3

# ── Optional: Finland data download ───────────────────────────────────────────
# pip install kaggle==1.6.14

# ── Optional: Knowledge Graph (Phase 5) ───────────────────────────────────────
# Sparksee 5.2.3 is installed via scripts/install_sparksee.sh — not pip.
# The Python bridge uses JPype:
# pip install JPype1==1.5.0
